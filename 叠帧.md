# 叠帧

This file has been updated to fix LaTeX formula rendering issues. The corrections include the following replacements:

- Replaced escaped underscores in math environments:
  - $T\_i$ -> $T_i$
  - $P\_i$ -> $P_i$
  - $P\_{world}$ -> $P_{world}$
  - $T\_0^{-1}$ -> $T_0^{-1}$
  - $P\_0$ -> $P_0$

The rendered formulas now appear correctly. 

Additional content and notes can be added here as necessary.